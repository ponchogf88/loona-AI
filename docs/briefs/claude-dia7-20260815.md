# Brief Claude — Día 7 loop (`wB:p2`)

**Fecha:** 2026-08-15 02:36 MTY  
**Write-path:** solo `runtime/`.

Grok acaba de re-medir: `POST /api/cast/card {"text":"Buenos dias."}` → 200 Choza, clima 26° MTY. light_reply ya no miente. No reabrir Día 6.

## P0 — un beat de casa

Cuando LOONA **habla** (`/api/chat` spoke, `/api/pet` talk, `/api/amigo`):

1. Voz Cast a Choza (ya está).
2. **También** la tarjeta (`cast_card`) el mismo beat — lockup + frase + clima. Un shot, no loop.
3. Si el user pide visión (`qué ves` / intent see): desktop snap + amigo. Eufy snap sigue NEED_HUMAN si no hay `EUFY_RTSP_URL`. No inventes RTSP.
4. Bind solo `127.0.0.1:8766`. Evidencia curl de **un** `/api/chat` o `/api/amigo` + que el card salió (log o file `card-*.jpg` nuevo).

No toques `hud/`. No pegues keys. Reporta DONE|NEED_HUMAN + curls + 1 línea.
