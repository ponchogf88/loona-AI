# Brief Claude — no te pares (`wB:p2`)

**Fecha:** 2026-08-14 14:27 MTY  
**Write-path:** solo `runtime/`.

Grok ya verificó (no repitas curls que pidan Allow si no hace falta):

- Chat Día 2: `POST /api/chat` 200 `spoke=true` `deepseek`. Cast Choza 200.
- Desktop snap 200 (82–91 KB).
- Cuenta Eufy SET → `account_configured:true`. `EUFY_RTSP_URL` ABSENT.
- Luz: NEED_HUMAN honesto (límite pyeufysecurity, no credenciales). Snap eufy 501.
- Bind `127.0.0.1:8766` PID nuevo OK. Dash API honestos.

## P0 ahora — Día 6 leftover, sin esperar RTSP

1. **Fix copy:** `pet.py` `light_reply()` todavía dice “cuando pongas la cuenta en el .env” aunque la cuenta YA está. Alinea el habla con el hint real (falta cliente P2P / RTSP, no la cuenta).
2. **Día 6 PLAN_CASA_APEX:** al hablar, el Hub puede mostrar una **tarjeta** (lockup + texto corto + clima). Cast imagen/HTML estático vía `media_lan` + `play_media` image, **un** beat, no loop. Evidencia curl o log. Si pychromecast solo lleva el mp3 hoy, deja un endpoint `POST /api/cast/card` honesto y un cast de prueba.
3. No inventes P2P. No pegues keys/RTSP. No toques `hud/css` `hud/js`.
4. Reinicia solo si cambias código. Bind solo `127.0.0.1`.

Reporta `DONE|BLOCKED|NEED_HUMAN` + curls recortados + 1 línea.
